version: '3'

services:
    mongo:
        image: 'mongo:3.4.1'
        ports:
            - '27017:27017'
        volumes:
            - 'mongo:/data/db'
    goober:
        tty: true
        stdin_open: true
        depends_on:
            - 'mongo'
        build: .
        command: node server.js
        ports:
            - '8080:8080'
        env_file:
            - .env.list
        environment:
            - MONGODB_URI=mongodb://mongo:27017/
volumes:
    mongo:
